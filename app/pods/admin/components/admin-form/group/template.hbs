{{!--

  NOTE: For the `readonly`, `textarea`, and `textbox` components, we are using `id` instead of
        `elementId`. This is a workaround for a bug in Ember 3.11.

        See: https://github.com/emberjs/ember.js/issues/18147

--}}
<div class={{if this.useDefaultMargin "mb-6"}} ...attributes>
  {{yield
    (
      hash
        datepicker=(component "admin/components/admin-form/datepicker" id=this.uniqueId errors=this.errors)
        help=(component "admin/components/admin-form/help")
        label=(component "admin/components/admin-form/label" for=this.uniqueId errors=this.errors)
        readonly=(component "ui-textbox" id=this.uniqueId readonly=true)
        textarea=(component "ui-textarea" id=this.uniqueId errors=this.errors)
        textbox=(component "ui-textbox" id=this.uniqueId errors=this.errors)
        validationErrors=(component "admin/components/admin-form/validation-errors" errors=this.errors)
    )
  }}
</div>